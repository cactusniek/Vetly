services:
    sync:
        build:
            context: .
            dockerfile: sync/Dockerfile
        restart: unless-stopped
        env_file:
            - ./sync/.env
        volumes:
            - sync-data:/app/sync/data
        ports:
            - '127.0.0.1:4000:4000'

volumes:
    sync-data:
